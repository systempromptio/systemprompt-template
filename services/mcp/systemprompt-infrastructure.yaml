mcp_servers:
  systemprompt-infrastructure:
    binary: "systemprompt-infrastructure"
    package: "systemprompt-mcp-infrastructure"
    port: 5002
    endpoint: "http://localhost:8080/api/v1/mcp/systemprompt-infrastructure/mcp"
    enabled: false
    display_in_web: true
    description: "SystemPrompt Infrastructure MCP Server - Cloud infrastructure management, sync, deploy, and monitor"

    oauth:
      required: true
      scopes: ["admin"]
      audience: "mcp"
      client_id: null
