mcp_servers:
  systemprompt-admin:
    binary: "systemprompt-admin"
    package: "systemprompt-admin"
    port: 5001
    endpoint: "http://localhost:8080/api/v1/mcp/systemprompt-admin/mcp"
    enabled: false
    display_in_web: true
    description: "SystemPrompt Admin MCP Server - Administrative tools for system management"

    oauth:
      required: true
      scopes: ["admin"]
      audience: "mcp"
      client_id: null
