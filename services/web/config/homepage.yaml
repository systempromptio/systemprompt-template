# Homepage Configuration
#
# The `demos` section (quick_start + categories) is populated at load time
# from the demo/ directory on disk — see extensions/web/site/src/homepage/demo_scanner.rs.
# Only `title` and `subtitle` here are authoritative; anything else is overwritten.

hero:
  title: "Run the Platform"
  title_highlight: "in 60 Seconds."
  subtitle: "Forty-plus runnable demo scripts walk through governance, agents, MCP, analytics, and more. Build the binary, start services, copy a command, and watch the platform work end-to-end."
  eyebrow: "Enterprise Demo — Interactive Getting Started"
  cta: "Jump to Quick Start"
  cta_url: "#quick-start"
  cta_secondary: "Read the Docs"
  cta_secondary_url: "/documentation"

demos:
  title: "Demo Catalogue"
  subtitle: "Every script below is a real, runnable shell script in demo/. Click a card to see the commands it runs, then copy them into your terminal."
