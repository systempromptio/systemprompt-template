id: sfcc_api_classes
name: "SFCC API Classes"
description: "Use when answering questions about SFCC Script API classes (dw.*), methods, properties, custom attributes, custom objects, globally available functions, or XSD schemas for import/export. MUST be read BEFORE answering about SFCC API availability, class methods, global functions, custom attribute definitions, custom object types, or object structure. This is the authoritative offline reference (bundled class corpus + extraction scripts); for the full `b2c docs` CLI command surface use the b2c-docs skill. Do not rely on assumptions — always verify against this reference."
enabled: true
file: SKILL.md
tags:
  - development
  - build
  - sfcc
category: development
display_category: "Development — B2C Build"
