id: sfnext_test_ids
name: "Storefront Next Test IDs"
description: "Add stable `data-testid` attributes to Storefront Next rendered elements (React feature components, route files, extension components, Page Designer components, shadcn `ui/*` primitives, skeletons, modals, list items, form inputs / textareas / checkboxes / radio groups / selects) so unit tests and Playwright e2e have durable selectors. Use whenever a new component/route/extension is added, a form/input is made test-addressable, an existing element needs a stable selector, or a diff introduces interactive UI without test hooks. Shift-left testability — apply during implementation, not after."
enabled: true
file: SKILL.md
tags:
  - development
  - build
  - storefront-next
category: development
display_category: "Development — Storefront Next"
