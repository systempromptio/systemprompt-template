{
  "db_name": "PostgreSQL",
  "query": "\n        WITH spine AS (SELECT generate_series(0, $3::INT - 1) AS bucket),\n             counts AS (\n                 SELECT LEAST(\n                            $3::INT - 1,\n                            FLOOR(EXTRACT(EPOCH FROM (r.created_at - $1)) / $4::FLOAT8)::INT\n                        ) AS bucket,\n                        COUNT(*)::BIGINT AS requests\n                 FROM ai_requests r\n                 WHERE NOT r.synthetic\n                   AND r.created_at >= $1\n                   AND r.created_at < $2\n                 GROUP BY 1\n             )\n        SELECT COALESCE(counts.requests, 0)::BIGINT AS \"requests!\"\n        FROM spine LEFT JOIN counts ON counts.bucket = spine.bucket\n        ORDER BY spine.bucket\n        ",
  "describe": {
    "columns": [
      {
        "ordinal": 0,
        "name": "requests!",
        "type_info": "Int8",
        "origin": "Expression"
      }
    ],
    "parameters": {
      "Left": [
        "Timestamptz",
        "Timestamptz",
        "Int4",
        "Float8"
      ]
    },
    "nullable": [
      null
    ]
  },
  "hash": "cf9a97aa3b5acfbffde850938d16cacf823b2d1c470a1a4d1f5d7315c979fac3"
}
