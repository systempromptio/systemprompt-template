[package]
name = "gateway-integration-tests"
version.workspace = true
edition.workspace = true
publish = false

[lints]
workspace = true

[dependencies]
# The profile gateway types (GatewayConfig, GatewayRoute, RouteRequirements,
# ProviderRegistry, ModelGovernance) via the facade's `models` module — pure
# config, no database or runtime needed.
systemprompt = { workspace = true }
serde_yaml = { workspace = true }
