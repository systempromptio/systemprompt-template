gateway:
  enabled: false
  routes:
  - id: contract-claude
    model_pattern: claude-*
    provider: anthropic
  - id: contract-gpt
    model_pattern: gpt-*
    provider: anthropic
  default_provider: null
  allow_unlisted_models: false
  auth_scheme: bearer
  inference_path_prefix: /v1
