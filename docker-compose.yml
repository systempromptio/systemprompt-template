# SystemPrompt Template - Local Development
#
# NOTE: This template uses systemprompt-db for database provisioning.
# Start systemprompt-db before running `just setup`:
#
#   cd ../systemprompt-db
#   docker-compose -f docker-compose.local.yml up -d
#
# This file is kept for any additional local services you may need.

services: {}
