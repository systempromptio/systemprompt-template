# Core MCP Module Manifest
# Model Context Protocol server management
name: mcp
version: "1.0.0"
display_name: "MCP Service Manager"
description: "Model Context Protocol server orchestration and management"
authors: ["systemprompt-os"]
license: "Apache-2.0"
weight: 1
category: core
dependencies: []

schemas:
  - file: "schema/mcp_tool_executions.sql"
    table: "mcp_tool_executions"
    required_columns: ["mcp_execution_id", "tool_name", "mcp_server_name", "started_at", "status"]

tables_created:
  - "mcp_tool_executions"

seeds: []

api:
  enabled: true
  path_prefix: "/api/v1/mcp"
