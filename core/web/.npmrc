# React 19 Early Adoption
# Several dependencies (react-helmet-async, recharts) haven't updated peer deps to React 19 yet
# React 19 is API-compatible with React 18, so this is safe
# Remove this once dependencies catch up
legacy-peer-deps=true
