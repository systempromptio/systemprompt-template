{{#> layout title=title page=page}}
    {{#*inline "head_extra"}}{{/inline}}
    {{#*inline "content"}}
    {{> components/page-header title=title}}
    <section aria-label="Overview stats">
        <div class="stat-grid">
            {{> components/stat-card label="Requests (events)" value=stats.total_events}}
            {{> components/stat-card label="Sessions" value=stats.total_sessions}}
            {{> components/stat-card label="Tool Uses" value=stats.tool_uses}}
            {{> components/stat-card label="Errors" value=stats.errors variant="danger"}}
        </div>
    </section>
    {{/inline}}
    {{#*inline "scripts"}}{{/inline}}
{{/layout}}
